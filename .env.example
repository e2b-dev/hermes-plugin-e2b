# Copy the value into ~/.hermes/.env (or the active profile's .env).
# The key authenticates the E2B SDK on this machine. It is never sent into a
# sandbox, never uploaded, and never written to a command line.
E2B_API_KEY=
