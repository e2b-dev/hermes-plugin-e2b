# E2B terminal backend for Hermes Agent

A standalone Hermes Agent terminal-environment plugin that runs terminal,
file, and code-execution tools inside E2B cloud sandboxes.

The plugin registers the `e2b` backend through Hermes' documented terminal
environment provider API. See the repository documentation added in the next
commit for installation, lifecycle, persistence, security, and attribution.
